version: "3"

services:
  ftx-back:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
      - "127.0.0.1:8081:8081" # 预留给pprof
    networks:
      - ftx
    volumes:
      - /var/ftx-wsite/logs:/www/ftx-wsite/logs
    links:
      - mysql
    depends_on:
      - mysql
  mysql:
    image: "mysql:8.0"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_ROOT_HOST: "%"
    expose:
      - "3306"
    networks:
      - ftx
    volumes:
      - /etc/localtime:/etc/localtime
      - /var/mysql/cnf:/etc/mysql/conf.d
      - /var/mysql/data:/var/lib/mysql

networks:
  ftx:
    driver: bridge